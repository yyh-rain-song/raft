
#include "identity.h"

int main() {
    yyh_raft::follower example;
    //example.identity_transfer();
    boost::timer Time;
    std::cout << Time.elapsed();
    return 0;
}
/*
 * there's something need to be bind, but I don't know where to put them
 * right now
 * */
